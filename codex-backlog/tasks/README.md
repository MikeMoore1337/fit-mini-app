# Final pre-release tasks

Основной порядок числовых tasks не меняется. Добавлены `59A` после `59` и Admin-последовательность
`69 -> 69A -> 70 -> 70A -> 71 -> 71A -> 72`.

`00-46` и integration gate `46A-46J`, включая `46C.1-46C.6`, уже завершены.
Next: `47-profile-account-experience.md`.

Use one fresh Codex chat per task and one logical commit per implementation stage.

After task `93`, do not add new pre-release features.
Move ideas to the post-release discovery backlog and validate with live users.
