# Порядок выполнения release backlog v18

## Completed

`00-67`, включая буквенные подзадачи. Task-файлы находятся в `tasks/done/`.

## Current

```text
68 Demo Mode foundation и три подготовленных сценария
```

После завершения `68`:

```text
69 -> 70 -> 71 -> 72 -> 73 -> 74 -> 75
 -> 76 skill-aware retrospective audit -> 76A adversarial gate
 -> 77 real-user results checkpoint
 -> 78 production readiness
 -> 79 final go/no-go
```

`DESIGN_V2_1` остаётся единственным active production source. Никакая task не запускает следующую автоматически.
